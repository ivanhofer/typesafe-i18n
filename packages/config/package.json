{
  "name": "@typesafe-i18n/config",
  "scripts": {
    "test": "tsc --noEmit",
    "generate:json-schema": "rimraf \"../../schema\" && mkdir \"../../schema\" && typescript-json-schema --noExtraProps ./tsconfig.json GeneratorConfig > \"../../schema/typesafe-i18n.json\""
  },
  "devDependencies": {
    "ajv": "^6.12.6",
    "kleur": "^4.1.5",
    "typescript-json-schema": "^0.54.0"
  },
  "type": "module"
}