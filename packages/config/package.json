{
  "name": "@typesafe-i18n/config",
  "scripts": {
    "generate:json-schema": "rm -rf ../../schema && mkdir ../../schema && typescript-json-schema --noExtraProps ./tsconfig.json GeneratorConfig > ../../schema/typesafe-i18n.json"
  },
  "devDependencies": {
    "ajv": "^6.12.6",
    "kleur": "^4.1.4",
    "typescript-json-schema": "^0.53.0"
  }
}